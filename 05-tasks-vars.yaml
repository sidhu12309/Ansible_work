---
- name: task level variable
  hosts: web
  tasks: 
   - name: print the info
      ansible.builtin.debug:
        msg: "Hi, I am Learning {{ Course }} Duration: {{ Duration }} trainer: {{
          Trainer }}"
